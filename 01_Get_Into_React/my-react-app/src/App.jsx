import './App.css';

// React component: 
// The entire react application can divided into small parts called "component".
// It's a simple JS function (it's name started with capital letter, here 'App' not 'app')
// Each component can contain markup (JSX syntax.)
function App() {
  return (
    <div>
      <h1>Hello React 👋</h1>
      <p>This is my first React app</p>
    </div>
  );
}

export default App; // This is a standard JavaScript syntax to export function. (Here App is a react component)